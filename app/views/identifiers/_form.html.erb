<%= form_for(@identifier) do |f| %>
  <% if @identifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@identifier.errors.count, "error") %> prohibited this identifier from being saved:</h2>

      <ul>
      <% @identifier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :real_id %><br />
    <%= f.text_field :real_id %>
  </div>
  <div class="field">
    <%= f.label :generated_id %><br />
    <%= f.text_field :generated_id %>
  </div>
    <% types_array = IdType.all.map { |i| [i.name, i.id] } %>
  <div class="field">
    <%= f.label :type_id %><br />
    <%= f.select :type_id, types_array %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
