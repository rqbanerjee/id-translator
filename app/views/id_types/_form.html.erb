<%= form_for(@id_type) do |f| %>
  <% if @id_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@id_type.errors.count, "error") %> prohibited this id_type from being saved:</h2>

      <ul>
      <% @id_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :length %><br />
    <%= f.number_field :length %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
